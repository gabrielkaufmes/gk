{
  "name": "logistic-optimizer",
  "version": "0.1.0",
  "description": "Logistic Optimizer — Electron desktop app for Elwiz",
  "main": "electron/main.cjs",
  "author": "Gabriel Kaufmes",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "electron .",
    "start": "electron .",
    "dist:win": "electron-builder --win",
    "test:planner": "node planner/test/e2e.mjs",
    "build:planner-engine": "esbuild planner/src/engine/index.js --bundle --format=iife --global-name=PlannerEngine --outfile=public/vendor/planner-engine.js"
  },
  "dependencies": {
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.0"
  },
  "build": {
    "appId": "com.elwiz.logisticoptimizer",
    "productName": "Logistic Optimizer",
    "directories": {
      "output": "release"
    },
    "files": [
      "electron/**/*",
      "public/**/*",
      "config/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false
    }
  }
}
